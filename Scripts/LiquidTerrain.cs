using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LiquidTerrain : MonoBehaviour
{
    // entity --> should set speedModifier to Some Value when center point is within liquid collider
    // is a script needed for this? 
    // could do isTouchingLayer(LiquidTerrain)
}
